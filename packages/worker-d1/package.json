{
  "name": "@spike/worker-d1",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --var VERSION:${DEPLOY_VERSION:-$(git describe --tags --always --dirty)}",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260515.1",
    "wrangler": "^4.91.0"
  }
}
